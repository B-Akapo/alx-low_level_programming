#include "main.h"

/**
* _abs - returns the absolute value of a number given to it
* @num: a given value
* Return: return 0 if successful 1 if otherwise
*/

int _abs(int num)
{
	if (num > 0)
	{
		return (num);
	}
	else if (n < 0)
	{
		return (-(num));
	}
	else
	{
		return (num);
	}
}
